package parts;

import org.apache.log4j.Logger;

/**
 * logger to log, immutable type.
 * @author 1170300229
 *
 */
public class Log {
  public static Logger logger = Logger.getLogger(Log.class);
  //Abstract Function:
  //      AF(logger) = a logger 
  // Representation invariant:
  //      RI(logger) = a valid logger
  // Safety from rep exposure:

}
